---
// Only render viewport breakpoint indicator in development mode
const isDevelopment = import.meta.env.MODE === "development";

if (!isDevelopment) {
  return;
}
---

<div
  class="fixed top-0 left-0 z-50 flex w-[30px] items-center justify-center bg-[#FF71CE] py-[2.5px] text-[12px] font-bold text-slate-900 uppercase sm:bg-[#01CDFE] md:bg-[#05FFA1] lg:bg-[#FFD156] xl:bg-[#B967FF] 2xl:bg-[#FFFB96]"
>
  <span class="block sm:hidden">all</span>
  <span class="hidden sm:block md:hidden">sm</span>
  <span class="hidden md:block lg:hidden">md</span>
  <span class="hidden lg:block xl:hidden">lg</span>
  <span class="hidden xl:block 2xl:hidden">xl</span>
  <span class="hidden 2xl:block">2xl</span>
</div>
