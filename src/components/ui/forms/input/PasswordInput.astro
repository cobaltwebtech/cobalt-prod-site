---
const { label = "Password", forgot, id, errorId, content } = Astro.props;

interface Props {
  label?: string;
  forgot?: boolean;
  id?: string;
  errorId?: string;
  content?: string;
}
---

<div>
  <div class="flex items-center justify-between">
    <label
      for={id}
      class="mb-2 block text-sm text-slate-800 dark:text-slate-200"
      >{label}</label
    >
    {
      forgot ? (
        <button
          class="rounded-lg text-sm font-medium text-orange-400 decoration-2 ring-zinc-500 outline-none hover:underline focus-visible:ring dark:text-orange-400 dark:ring-zinc-200 dark:focus:ring-1 dark:focus:outline-none"
          data-hs-overlay="#hs-toggle-between-modals-recover-modal"
        >
          Forgot password?
        </button>
      ) : (
        ""
      )
    }
  </div>
  <div class="relative">
    <input
      type="password"
      id={id}
      name="password"
      class="block w-full rounded-lg border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-700 focus:border-slate-200 focus:ring focus:ring-slate-400 focus:outline-none disabled:pointer-events-none disabled:opacity-50 dark:border-slate-600 dark:bg-slate-700/30 dark:text-slate-300 dark:focus:ring-1"
      required
      aria-describedby={errorId}
    />
    <div class="pointer-events-none absolute inset-y-0 end-0 hidden pe-3">
      <svg
        class="h-5 w-5 text-red-500"
        width="16"
        height="16"
        fill="currentColor"
        viewBox="0 0 16 16"
        aria-hidden="true"
      >
        <path
          d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM8 4a.905.905 0 0 0-.9.995l.35 3.507a.552.552 0 0 0 1.1 0l.35-3.507A.905.905 0 0 0 8 4zm.002 6a1 1 0 1 0 0 2 1 1 0 0 0 0-2z"
        ></path>
      </svg>
    </div>
  </div>
  <p class="mt-2 hidden text-xs text-red-600" id={errorId}>
    {content}
  </p>
</div>
