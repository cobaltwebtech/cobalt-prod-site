---
// Import the necessary dependencies
import FooterSocialLink from "../../ui/links/FooterSocialLink.astro";
import SecondaryCTA from "@/components/ui/buttons/SecondaryCTA.astro";
import enStrings from "@/utils/navigation.ts";
import Icon from "../../ui/icons/Icon.astro";
import BrandLogo from "@/components/BrandLogo.astro";
import { SITE } from "@/data_files/constants";

// Select the correct translation based on the page's lang prop:
const strings = enStrings;

// Define the variables that will be used in this component
const sectionThreeTitle: string = "Hours of Operation";
const sectionThreeDays: string = "Monday - Friday";
const sectionThreeHours: string = "9AM - 5PM (US Central Time)";
---

<footer class="w-full bg-cobalt-50 dark:bg-cobalt-900">
  <div
    class="mx-auto w-full max-w-[85rem] px-4 py-10 sm:px-6 lg:px-16 lg:pt-20 2xl:max-w-screen-2xl"
  >
    <div class="grid grid-cols-2 gap-6 md:grid-cols-4 lg:grid-cols-5">
      <div class="col-span-full lg:col-span-1">
        <!-- Brand Logo -->
        <BrandLogo class="h-auto w-32" />
      </div>
      <!-- An array of links for Offerings and Company sections -->
      {
        enStrings.footerLinks.map((section) => (
          <div class="col-span-1">
            <h3 class="font-bold text-slate-800 dark:text-slate-200">
              {section.section}
            </h3>
            <ul class="mt-3 grid space-y-3">
              {section.links.map((link, index) => (
                <li>
                  <a
                    href={link.url}
                    class="inline-flex gap-x-2 rounded-lg text-slate-600 outline-none ring-slate-500 transition duration-300 hover:text-orange-700 focus-visible:ring dark:text-slate-400 dark:ring-slate-200 dark:hover:text-slate-300 dark:focus:outline-none"
                  >
                    {link.name}
                  </a>
                </li>
              ))}
            </ul>
          </div>
        ))
      }

      <div class="col-span-2">
        <h3 class="mb-2 font-bold text-slate-800 dark:text-slate-200">
          {sectionThreeTitle}
        </h3>
        <p class="text-slate-600 dark:text-slate-400">
          {sectionThreeDays}
        </p>
        <p class="text-slate-600 dark:text-slate-400">
          {sectionThreeHours}
        </p>
      </div>
    </div>

    <div
      class="mt-9 grid gap-y-2 sm:mt-12 sm:flex sm:items-center sm:justify-between sm:gap-y-0"
    >
      <div>
        <p class="mb-2 text-sm text-slate-800 dark:text-slate-200">
          © <span id="current-year"></span> - <span class="text-xl font-bold"
            >{SITE.title}</span
          >
        </p>
        <p class="mb-4 text-sm text-slate-600 dark:text-slate-400">
          Based, built, and coded in Austin, Texas USA.
        </p>
        <div class="flex gap-2.5">
          <Icon name="usFlag" />
          <Icon name="txFlag" />
        </div>
      </div>

      <!-- Social Brands -->
      <div>
        <FooterSocialLink url={enStrings.socialLinks.x}
          ><Icon name="xFooter" /></FooterSocialLink
        >
        <FooterSocialLink url={enStrings.socialLinks.github}
          ><Icon name="githubFooter" />
        </FooterSocialLink>
      </div>
    </div>

    <script>
      const year = new Date().getFullYear();
      const element = document.getElementById("current-year");
      element!.innerText = year.toString();
    </script>
  </div>
</footer>
